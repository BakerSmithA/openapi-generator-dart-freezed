{{#models}}
    {{#model}}
    {{>header}}
    // ignore_for_file: unused_element{{#useFreezed}}, invalid_annotation_target{{/useFreezed}}
    {{! 
    If this model is part of a freezed union type, there is no need to create
    the model here - it will be automatically generated by the freezed package.
    }}
    {{^vendorExtensions.x-is-freezed-union-member}}
    {{^useFreezed}}
        {{#imports}}
import '{{.}}';
        {{/imports}}
    {{/useFreezed}}
{{#isEnum}}{{>enum}}{{/isEnum}}{{^isEnum}}{{>class}}{{/isEnum}}
    {{/vendorExtensions.x-is-freezed-union-member}}
    {{/model}}
{{/models}}